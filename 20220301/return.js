function laskeKaksiLukuaYhteen(luku1, luku2) {
    return luku1 + luku2;
}

let summa = laskeKaksiLukuaYhteen(2, 3);

function checkDate() {
    let message = '';

    // jos virhe
    if () {
        message = VIRHE_ILMOITUS;
    }

    return message
}

// kutsutaan checkDate()-funktiota
let errormessage = checkDate(value, century);
